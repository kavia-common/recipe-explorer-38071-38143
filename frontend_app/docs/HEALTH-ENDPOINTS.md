# Health Endpoints

Available without bundling (zero-bundle):
- http://localhost:3000/healthz.html
- http://localhost:3000/healthz
- http://localhost:3000/health.json

These are created during prestart and present in public/.
