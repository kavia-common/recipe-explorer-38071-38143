Health endpoints available immediately (zero-bundle):
- /healthz.html  (HTML)
- /healthz       (plain text)
- /health.json   (JSON)

These are served by the static server and exist before any JS bundling.
