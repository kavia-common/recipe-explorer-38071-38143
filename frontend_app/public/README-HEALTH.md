Health endpoints served without bundling (zero-bundle):
- /healthz.html (HTML)
- /healthz (text)
- /health.json (JSON)

These are static files under public/ and are available regardless of the React dev server status.
