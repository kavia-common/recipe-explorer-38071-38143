# PUBLIC_INTERFACE
# Example environment configuration for frontend_app (React) in CI/containers.
# Copy to .env and adjust as needed. Do not commit secrets.

# Networking
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000
REACT_APP_FRONTEND_URL=http://localhost:3000
REACT_APP_BACKEND_URL=http://localhost:5000
REACT_APP_API_BASE=/api
REACT_APP_WS_URL=ws://localhost:3000
REACT_APP_NODE_ENV=development
REACT_APP_TRUST_PROXY=true

# Stability and memory caps
# Disable source maps to reduce memory usage in dev server
REACT_APP_ENABLE_SOURCE_MAPS=false
# Cap Node memory to avoid exit code 137 (OOM) in CI
NODE_OPTIONS=--max-old-space-size=256
# Avoid opening a browser in CI
BROWSER=none
# Prevent chokidar polling overhead in containers
CHOKIDAR_USEPOLLING=false
# Avoid websocket port negotiation issues in containers
WDS_SOCKET_PORT=0

# Logging and health
REACT_APP_LOG_LEVEL=info
REACT_APP_HEALTHCHECK_PATH=/healthz.html

# Feature flags
REACT_APP_FEATURE_FLAGS={}
REACT_APP_EXPERIMENTS_ENABLED=false

# Next telemetry (not used by CRA, but often set globally)
REACT_APP_NEXT_TELEMETRY_DISABLED=1
