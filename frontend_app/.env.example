# PUBLIC_INTERFACE
# CI-friendly defaults to avoid OOM (exit code 137) and stabilize webpack dev server.
# Copy this file to .env in the same directory to apply.

# Server binding
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000

# Disable browser auto-open and reduce fs watching overhead
BROWSER=none
CHOKIDAR_USEPOLLING=false
WDS_SOCKET_PORT=0

# Keep Node memory capped to avoid OOM
NODE_OPTIONS=--max-old-space-size=256

# Disable source maps to reduce memory and CPU usage
REACT_APP_ENABLE_SOURCE_MAPS=false
GENERATE_SOURCEMAP=false

# Healthcheck path (served statically so it works before bundle compiles)
REACT_APP_HEALTHCHECK_PATH=/healthz.html

# Optional flags for CI static-only mode (zero-bundle)
#CI_STATIC_ONLY=1

# App URLs (sample placeholders; adjust as needed)
REACT_APP_API_BASE=http://localhost:4000
REACT_APP_BACKEND_URL=http://localhost:4000
REACT_APP_FRONTEND_URL=http://localhost:3000
REACT_APP_WS_URL=ws://localhost:3000/ws

# Other toggles
REACT_APP_NODE_ENV=development
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_TRUST_PROXY=true
REACT_APP_LOG_LEVEL=info
REACT_APP_FEATURE_FLAGS={}
REACT_APP_EXPERIMENTS_ENABLED=false
