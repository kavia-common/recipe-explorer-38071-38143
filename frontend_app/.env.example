# PUBLIC_INTERFACE
# Sample environment for stable startup in CI/containers (prevents exit code 137/OOM)
# Copy this to .env if needed and adjust values for your environment.

# Bindings
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000

# Stability flags
BROWSER=none
CHOKIDAR_USEPOLLING=false
WDS_SOCKET_PORT=0

# Memory caps for Node (in MB)
NODE_OPTIONS=--max-old-space-size=256

# Disable source maps to reduce memory during dev/build
REACT_APP_ENABLE_SOURCE_MAPS=false
GENERATE_SOURCEMAP=false

# Healthcheck
REACT_APP_HEALTHCHECK_PATH=/healthz.html

# Other optional variables referenced by work item
REACT_APP_API_BASE=
REACT_APP_BACKEND_URL=
REACT_APP_FRONTEND_URL=
REACT_APP_WS_URL=
REACT_APP_NODE_ENV=development
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_TRUST_PROXY=true
REACT_APP_LOG_LEVEL=info
REACT_APP_FEATURE_FLAGS=
REACT_APP_EXPERIMENTS_ENABLED=false
