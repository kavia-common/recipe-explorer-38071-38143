# PUBLIC_INTERFACE
# This file documents environment variables for the frontend_app.
# Copy to .env and adjust values as needed. Do NOT commit secrets.

# Server binding
HOST=0.0.0.0
REACT_APP_PORT=3000
PORT=3000

# CI/Dev server behavior
BROWSER=none
CHOKIDAR_USEPOLLING=false
WDS_SOCKET_PORT=0

# Memory limits (keep low to avoid exit 137/OOM in CI)
NODE_OPTIONS=--max-old-space-size=256

# Disable source maps in CI to reduce memory and disk usage
REACT_APP_ENABLE_SOURCE_MAPS=false

# Health endpoint path (must exist in public/)
REACT_APP_HEALTHCHECK_PATH=/healthz.html

# Feature flags (optional)
REACT_APP_NODE_ENV=development
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_TRUST_PROXY=true
REACT_APP_LOG_LEVEL=info
REACT_APP_FEATURE_FLAGS={}
REACT_APP_EXPERIMENTS_ENABLED=false

# Networking (if backends are used; placeholder)
REACT_APP_API_BASE=
REACT_APP_BACKEND_URL=
REACT_APP_FRONTEND_URL=
REACT_APP_WS_URL=
