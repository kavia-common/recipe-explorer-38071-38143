# PUBLIC_INTERFACE
# Environment template for frontend_app (React CRA)
# Purpose: Provide stable defaults for CI/containers to avoid exit code 137 (OOM) and websocket/BROWSER issues.

# Server bind/port
HOST=0.0.0.0
REACT_APP_PORT=3000
PORT=3000

# Keep browser from auto-opening in CI
BROWSER=none

# Disable react-scripts source maps to reduce memory use
REACT_APP_ENABLE_SOURCE_MAPS=false
GENERATE_SOURCEMAP=false

# Webpack dev server socket port negotiation issues inside containers
WDS_SOCKET_PORT=0

# File watcher polling can be expensive; keep it off by default
CHOKIDAR_USEPOLLING=false

# Cap Node memory to avoid OOM (Exit 137)
NODE_OPTIONS=--max-old-space-size=256

# Healthcheck path served statically (zero-bundle)
REACT_APP_HEALTHCHECK_PATH=/healthz.html

# Logging level for any custom logs
REACT_APP_LOG_LEVEL=info

# Optional URLs (not strictly used in this template yet)
REACT_APP_API_BASE=
REACT_APP_BACKEND_URL=
REACT_APP_FRONTEND_URL=
REACT_APP_WS_URL=
REACT_APP_NODE_ENV=development
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_TRUST_PROXY=true
REACT_APP_FEATURE_FLAGS=
REACT_APP_EXPERIMENTS_ENABLED=false
