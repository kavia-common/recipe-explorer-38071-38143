# PUBLIC_INTERFACE
# Sample environment for stable CI/containers to avoid exit code 137 (OOM) and websocket issues.

# Core
HOST=0.0.0.0
PORT=3000
BROWSER=none
CHOKIDAR_USEPOLLING=false
WDS_SOCKET_PORT=0

# Memory tuning
# Cap Node.js memory to keep CRA dev server from exceeding container limits.
NODE_OPTIONS=--max-old-space-size=256

# React build options
# Disable source maps to reduce memory and disk usage in CI/dev servers.
REACT_APP_ENABLE_SOURCE_MAPS=false
GENERATE_SOURCEMAP=false

# Health check path (served statically from public/healthz.html)
REACT_APP_HEALTHCHECK_PATH=/healthz.html

# Optional: enable static-only mode (zero-bundle) for ultra-tight memory CI:
# CI_STATIC_ONLY=1

# Application URLs (adjust per environment; not required for health)
REACT_APP_API_BASE=
REACT_APP_BACKEND_URL=
REACT_APP_FRONTEND_URL=
REACT_APP_WS_URL=
REACT_APP_NODE_ENV=development
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_PORT=3000
REACT_APP_TRUST_PROXY=1
REACT_APP_LOG_LEVEL=info
REACT_APP_FEATURE_FLAGS=
REACT_APP_EXPERIMENTS_ENABLED=0
