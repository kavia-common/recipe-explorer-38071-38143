# Safe defaults for CI/containers to avoid exit code 137/OOM and ensure stable startup

# Server binding
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000

# Disable auto-opening browser and polling to reduce overhead
BROWSER=none
CHOKIDAR_USEPOLLING=false
WDS_SOCKET_PORT=0

# Source maps off to reduce memory usage
REACT_APP_ENABLE_SOURCE_MAPS=false
GENERATE_SOURCEMAP=false

# Cap Node memory (adjust down if extremely constrained)
NODE_OPTIONS=--max-old-space-size=256

# Health checks (served even before bundling)
REACT_APP_HEALTHCHECK_PATH=/healthz.html

# Runtime env placeholders (do not hardcode secrets; set in CI)
REACT_APP_API_BASE=
REACT_APP_BACKEND_URL=
REACT_APP_FRONTEND_URL=
REACT_APP_WS_URL=
REACT_APP_NODE_ENV=development
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_TRUST_PROXY=1
REACT_APP_LOG_LEVEL=info
REACT_APP_FEATURE_FLAGS=
REACT_APP_EXPERIMENTS_ENABLED=0
