# PUBLIC_INTERFACE
# Example environment for stable CI/containers (prevents exit 137 OOM)
# Copy to .env if needed and adjust values per environment.

# Server binding and port
HOST=0.0.0.0
REACT_APP_PORT=3000
PORT=3000

# Disable auto-opening browser and reduce file watching overhead
BROWSER=none
CHOKIDAR_USEPOLLING=false
WDS_SOCKET_PORT=0

# Memory cap for Node process (adjust lower if CI is extremely tight)
NODE_OPTIONS=--max-old-space-size=256

# Disable source maps to reduce memory usage (react-scripts respects GENERATE_SOURCEMAP)
REACT_APP_ENABLE_SOURCE_MAPS=false
GENERATE_SOURCEMAP=false

# Healthcheck path (served statically from public/)
REACT_APP_HEALTHCHECK_PATH=/healthz.html

# Optional: serve only static files in CI for zero-bundle readiness
# Set to "1" to avoid starting the React dev server (for minimal memory pipelines)
# CI_STATIC_ONLY=1

# Optional logging and flags (read by app code if present)
REACT_APP_LOG_LEVEL=info
REACT_APP_FEATURE_FLAGS={}
REACT_APP_EXPERIMENTS_ENABLED=false

# Backend URLs (placeholder for future integration)
REACT_APP_API_BASE=
REACT_APP_BACKEND_URL=
REACT_APP_FRONTEND_URL=
REACT_APP_WS_URL=
REACT_APP_NODE_ENV=development
REACT_APP_NEXT_TELEMETRY_DISABLED=1
